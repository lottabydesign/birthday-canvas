# macOS
.DS_Store

# Node / Next.js
node_modules/
.next/
out/
build/
dist/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Env
.env
.env.local
.env*.local

# Editor / OS
.vscode/
.idea/
*.swp

# Claude Code & Superpowers — local settings, not committed
.claude/settings.local.json
.superpowers/

# Project-specific: redundant originals at the kam project root
# These were moved/copied into site/public/media/ — keep on disk for safety,
# but don't commit duplicates.
/pics-kam.zip
/pics-kam/
/PHOTO-*.jpg
/PHOTO-*.JPG
/image*.png
/IMG_*.MOV
/IMG_*.HEIC
/VIDEO-*.mov
/VIDEO-*.MOV

# Inside the media folder: ignore unconverted source video originals
# (we ship the converted .mp4 / .mp3 versions instead).
site/public/media/*.MOV
site/public/media/*.mov
site/public/media/*.HEIC
site/public/media/*.heic

# Build artefacts that shouldn't ship
*.tsbuildinfo
next-env.d.ts
